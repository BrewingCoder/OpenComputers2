"""
DEPRECATED — do not use.

Launch MC for OC2 development via:
    ./gradlew runClient --console=plain

Game directory is run/client/ (mods, configs, saves, kubejs all live there).
"""
raise SystemExit(
    "launch_mc.py is deprecated.\n"
    "Use:  ./gradlew runClient --console=plain\n"
    "Game dir: run/client/"
)
